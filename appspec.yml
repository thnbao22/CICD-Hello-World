version: 0.0
os: linux

files:
  - source: /
    destination: /var/www/hello-world-flask-app

hooks:
  ApplicationStart:
    - location: scripts/start_python_app.sh
      timeout: 300
      runas: root